# 测试指南
## 安装
详见README，注意提前准备conan包管理工具
## 参数
**`config.yaml`**：关注pos_compressor，vel_compressor即可，其他的不用动

**各个参数具体描述**：
```bash
python main.py roundtrip --help
```

**输入**：支持输入单个h5或raw data file，也支持输入整个snapshot的文件夹，会自动寻找所有的数据文件并行压缩

**输出**：
压缩完成的文件包在`${WORK_DIR}/${dirname}/${filename}/compressed/`

解压完成的文件在`${WORK_DIR}/${dirname}/${filename}/reconstructed.h5`

总压缩率，各个部分的压缩率，运行时间，mse，psnr会在console上呈现，具体数据在`${WORK_DIR}/${dirname}/batch_metrics.json`

## 测试
**特别注意**：默认跑roundtrip，最后runtime也是包括压缩，解压和eval的全部时间。

仅挑选我认为值得测试对比的方法
### 全无损
`pos_compressor: pcodec`

`vel_compressor: pcodec`
```bash
python main.py roundtrip "${INPUT}" \
  --config "config.yaml" \
  --work-dir "${WORK_DIR}" \
  --force --clean-raw --metrics
```

### SZo有损
`pos_compressor: szo`

`vel_compressor: szo`

#### 无排序
```bash
python main.py roundtrip "${INPUT}" \
  --config "config.yaml" \
  --work-dir "${WORK_DIR}" \
  --rel-eb "${eb}" \
  --force --clean-raw --metrics
```

#### ID排序
```bash
python main.py roundtrip "${INPUT}" \
  --config "config.yaml" \
  --work-dir "${WORK_DIR}" \
  --rel-eb "${eb}" \
  --sort \
  --force --clean-raw --metrics
```

#### 三维ID排序
不建议作为主流方法测试，在新数据集上表现不如lossless
```bash
python main.py roundtrip "${INPUT}" \
  --config "config.yaml" \
  --work-dir "${WORK_DIR}" \
  --rel-eb "${eb}" \
  --sort --lattice-layout \
  --force --clean-raw --metrics
```

### LCP + SZo，自动位置排序
`pos_compressor: lcp`

`vel_compressor: szo`
```bash
python main.py roundtrip "${INPUT}" \
  --config "config.yaml" \
  --work-dir "${WORK_DIR}" \
  --rel-eb "${eb}" \
  --force --clean-raw --metrics
```